import { defineConfig } from 'vite'

export default defineConfig({
  // Intentionally not using @vitejs/plugin-react to avoid peer dependency
  // conflicts in this quickstart. JSX will be handled by esbuild.
})
