# Copy this file to .env and set your personal access token to avoid GitHub rate limiting
# Create a personal access token on GitHub with "repo" (or at least public_repo) scope.
# Then set it in a local .env file (DO NOT commit .env to source control).

VITE_GITHUB_TOKEN=ghp_xxxYOUR_TOKEN_xxx